# API Notes

The agents are expected to work through the CRM API rather than touching the database directly.

Use the helper scripts in `../Shared/helper-scripts` for routine calls so the shell commands remain small and deterministic.
